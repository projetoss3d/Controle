/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Permite servir atrás de qualquer host (self-hosted-friendly).
  output: "standalone",
};
export default nextConfig;
